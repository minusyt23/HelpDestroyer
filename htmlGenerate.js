/// HTML Generate
/// Construct an HTML file from an IML (Intermediate Markup Language) object
/// Is a worker, so that if anything is slowing down, it's not the main thread.

function htmlGenerate(imd) {

}

onmessage = function(event) {

}