/// RTF Generate
/// Construct an RTF file from an IMD (Intermediate Markup Language) object
/// Is a worker, so that if anything is slowing down, it's not the main thread.

function rftGenerate(imd) {

}

onmessage = function(event) {

}